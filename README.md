# iris-classification
A simple ML project that predicts the species of an Iris flower (Setosa, Versicolor, Virginica) based on sepal and petal measurements. The model is trained using a Decision Tree Classifier, and the program accepts user input directly through the terminal to generate predictions.
